# v2.4 版本说明

本文档记录当前代码快照（v2.4）的完整历史与实现范围。文档由 v2.1.md → v2.2.md 一路演进，现合并为单一 v2.4.md，保留 v2.1、v2.2、v2.4 各阶段内容。

- Python package version：`2.4.0`
- Git release tag：`v2.4`
- Release date：`2026-08-23`

- `## 1` – `## 7`：v2.1 历史记录（研究问题、主要变化、验证、五模型结果）。
- `## 8` – `## 10`：v2.2 记录（Affine/Spline 公平性消融、Jacobian/拉伸指标、Spline bins 消融）。
- `## 11` – `## 15`：v2.4 全局相交校验的可扩展性修复。
- `## 16`：版本与默认配置一致性修复。
- `## 17`：新增 00027 网格及失败诊断。
- `## 18`：优化器、rotation mixing 与更大 Spline-NVP 的 1+2+3 实验。
- `## 19`：Tutte、mean-value、ABF++ 与合法性筛选式 `auto` 初始化。
- `## 20`：v2.4 发布状态、默认配置和验证清单。

v2.4 的推进惯例：沿用 v2.1 → v2.2 → v2.4 的小版本推进。

---

# 第一部分：v2.1 历史

## 1. 当前研究问题

```text
给定同一份合法初始 UV：
Direct UV、SLIM、Affine-NVP 和 Spline-NVP
在 distortion、合法性、安全裕量和运行时间上有什么差异？
```

所有优化方法都要求初始 UV 满足：

```text
num_flipped = 0
num_intersections = 0
```

如果输入 UV 非法，程序会拒绝开始训练。NVP 仍然定位为合法参数化上的保向优化，而不是非法 UV 修复器。

## 2. v2.1 的主要变化

### 2.1 统一初始 UV

Direct UV、Affine-NVP、Spline-NVP 和 SLIM 都可以通过 `--initial-uv` 使用同一份初始参数化。程序会检查顶点数量、三角面拓扑和 UV 是否存在。

如果没有显式指定初始 UV：

```text
输入自带 UV -> 使用输入 UV
输入没有 UV -> 使用确定性的 Tutte 初始化
```

默认对共享初始 UV 做确定性几何尺度归一化，使 `median(abs(det J)) = 1`。该操作只是正的均匀缩放，不改变翻转或全局相交状态，并让所有方法从相同且尺度合理的 UV 开始。

### 2.2 统一训练器

Direct UV 和两类 NVP 共用 `surface_nvp/training/trainer.py`，共享：

- 训练目标。
- validation interval。
- latest-valid rollback。
- model 与 optimizer 状态恢复。
- best-valid checkpoint 选择。
- metrics 和运行时间记录。
- 固定随机种子和 deterministic PyTorch algorithms。
- plateau 时恢复 best-valid checkpoint 并降低学习率。

最终 checkpoint 按合法结果中的面积加权 Symmetric Dirichlet loss 选择。
优化采用自由边界，不固定或惩罚边界顶点相对初始 UV 的位移。

### 2.3 更可靠的 distortion 与 injectivity objective

训练使用按 3D triangle area 加权的 Symmetric Dirichlet：

```text
E_SD = sigma_1^2 + sigma_2^2
     + 1 / (sigma_1 + eps)^2
     + 1 / (sigma_2 + eps)^2
```

其中 `eps = 1e-4` 用于在接近完全塌缩时保持有限且非零的排斥梯度。

辅助 injectivity loss 使用相对于初始 Jacobian 中位尺度归一化的 soft barrier。最终合法性仍由 signed area 和非相邻三角形相交检查决定，barrier 不能替代离散 validation。

### 2.4 Spline-NVP

除 affine coupling 外，提供 monotonic rational-quadratic spline coupling：

- 基于 MIT 许可证的 `nflows`。
- 每个一维 spline 严格单调，导数为正。
- coupling layer 交替变换 x/y。
- 不使用 determinant 为负的坐标交换。
- 使用 identity initialization。
- 使用正尺度 UV domain normalization。
- bounded spline 外使用可学习的全局正尺度和平移，允许自由调整 UV 尺度且保持正 Jacobian determinant。
- 支持解析 inverse 和 log determinant。

命令：

```bash
python scripts/train_injective_nvp.py --input mesh.obj --output spline.obj --coupling-type spline --spline-bins 8 --iters 1000 --device cuda
```

### 2.5 SLIM baseline

`external/slim_runner/` 提供一个最小 libigl wrapper。它从 OBJ 的 `vt` 读取指定初始 UV，而不是像官方示例程序一样重新生成初始化。

```bash
cmake -S external/slim_runner -B build/slim_runner
cmake --build build/slim_runner --config Release
python scripts/run_slim.py --input mesh.obj --initial-uv init.obj --output slim.obj --executable build/slim_runner/Release/surface_nvp_slim.exe --iters 20
```

### 2.6 指标扩展

提供：

- `symmetric_dirichlet_area_weighted_mean`
- `symmetric_dirichlet_median`
- `symmetric_dirichlet_p90`
- `symmetric_dirichlet_p95`
- `symmetric_dirichlet_p99`
- `angle_distortion_p95_deg`
- `normalized_uv_area_min`
- `elapsed_seconds`

旧 v1 metrics 缺少这些字段时仍可被汇总，新增字段输出为空。

## 3. 当前代码结构

```text
surface_nvp/
  io/              OBJ 和可选 USD I/O、MeshData
  geometry/        拓扑邻接和边界环
  init_param/      Tutte 与共享初始 UV 解析
  models/          DirectUV、Affine coupling、Spline coupling、NVP2D
  losses/          weighted SD、Jacobian barrier、identity regularization
  injectivity/     signed area 和 triangle intersection
  training/        统一配置、训练器、rollback、metrics、summary
  visualization/   UV、loss、area、distortion 图

scripts/
  init_uv.py
  train_direct_uv.py
  train_injective_nvp.py
  run_slim.py
  run_benchmark.py
  merge_benchmark_summaries.py
  fit_nvp_to_target.py
  check_uv.py
  summarize_metrics.py

external/slim_runner/
tests/
```

## 4. 已完成验证

v2.1 发布前完成：

- Python test suite：24 tests passed。
- `compileall`：通过。
- Python wheel build 与 CLI entry-point packaging：通过。
- Affine-NVP CPU 冒烟：通过。
- Spline-NVP CPU 冒烟：通过。
- Direct UV CUDA 冒烟：通过。
- Spline-NVP CUDA 冒烟：通过。
- Direct/Affine/Spline 五模型 1000 iterations：全部 0 flip、0 intersection。
- SLIM wrapper Windows/VS2022 build：通过。
- SLIM 五模型端到端运行：四个合法；Cow 有 1069 个全局相交并按失败结果保留。
- 显式 `initial_uv`、geometry-scale config 保存和重放：通过。
- v1 metrics summary 兼容：通过。
- benchmark 配置签名与 stale metrics 防护：通过。
- Affine/Spline 五模型 seeds 0-4：50/50 合法，全部 0 flip、0 intersection。
- Affine/Spline inverse、完整 log determinant 和全局正尺度：通过。

这些验证覆盖功能、稳定性和发布回归。

## 5. 五模型正式结果

`data/output/` 中保留 v1 的五个模型历史实验：

```text
Balls
David328
NefertitiFace
Cow
Isis
```

这些结果对应旧 affine-NVP/direct objective，应作为历史 baseline，不应与当前结果混为同一实验设置。

v2.1 的 seed 0 正式结果分为三个保持原始 provenance 的 benchmark 根目录：

- `data/output/v2.1/seed0/balls/`：Balls。
- `data/output/v2.1/seed0/small/`：David328、NefertitiFace、Isis。
- `data/output/v2.1/seed0/cow/`：Cow。
- `data/output/v2.1/seed0/all/`：五模型合并 summary 和组件哈希 manifest，不复制或改写原始结果。

全部运行使用 Direct/Affine/Spline 1000 iterations、SLIM 20 iterations、seed 0、相同 Tutte 初始 UV、geometry scaling 和 CUDA validation。

Balls、seed 0 的核心结果如下：

| 方法 | 面积加权 SD | SD P95 | 平均角度畸变 | 最小 signed area | 合法 |
|---|---:|---:|---:|---:|---|
| Initial | 5.7546 | 25.5784 | 18.0813° | 0.006513 | 是 |
| Direct UV | 4.4134 | 6.8465 | 6.5509° | 0.021028 | 是 |
| Affine-NVP | 5.2019 | 12.9719 | 16.1704° | 0.007310 | 是 |
| Spline-NVP | 4.5696 | 7.1966 | 12.4578° | 0.019135 | 是 |
| SLIM | 4.2459 | 5.2338 | 6.0570° | 0.019100 | 是 |

其余四个模型的核心结果如下：

| 模型 | 方法 | 面积加权 SD | SD P95 | 全局相交 | 合法 |
|---|---|---:|---:|---:|---|
| David328 | Direct UV | 19.2116 | 114.6093 | 0 | 是 |
| David328 | Affine-NVP | 10.5992 | 31.0064 | 0 | 是 |
| David328 | Spline-NVP | 8.8961 | 23.9419 | 0 | 是 |
| David328 | SLIM | 7.2377 | 16.8663 | 0 | 是 |
| NefertitiFace | Direct UV | 4.0415 | 4.2274 | 0 | 是 |
| NefertitiFace | Affine-NVP | 4.4327 | 5.6591 | 0 | 是 |
| NefertitiFace | Spline-NVP | 4.0969 | 4.5512 | 0 | 是 |
| NefertitiFace | SLIM | 4.0362 | 4.2538 | 0 | 是 |
| Cow | Direct UV | 286.0013 | 266.4741 | 0 | 是 |
| Cow | Affine-NVP | 67.7577 | 355.3005 | 0 | 是 |
| Cow | Spline-NVP | 15.7950 | 70.8681 | 0 | 是 |
| Cow | SLIM | 4.0853 | 4.6236 | 1069 | 否 |
| Isis | Direct UV | 4.8885 | 5.8054 | 0 | 是 |
| Isis | Affine-NVP | 7.6356 | 18.1274 | 0 | 是 |
| Isis | Spline-NVP | 5.5539 | 10.5765 | 0 | 是 |
| Isis | SLIM | 4.5308 | 5.7038 | 0 | 是 |

除 Cow 的 SLIM 结果外，20 个优化结果中的其余 19 个均为 0 flip、0 nonfinite 和 0 non-adjacent intersection。Cow 的 SLIM 输出有 1069 个全局相交，因此即使其局部 SD 较低，也作为正式失败结果保留；Cow Direct 的 best-valid checkpoint 为 iteration 0，未改善共享初始 UV。完整精度和运行时间见 `data/output/v2.1/seed0/all/summary.csv`。

### 5.1 Affine/Spline 多 seed 统计

`data/output/v2.1/multiseed/` 保存 Affine-NVP 和 Spline-NVP 在 seeds 0-4 上的正式矩阵。全部 50 个结果独立复核为 0 flip、0 nonfinite 和 0 non-adjacent intersection。表中为面积加权 SD 和 SD P95 的 mean ± sample std：

| 模型 | Affine-NVP SD | Spline-NVP SD | Affine P95 | Spline P95 | Spline 相对改善 | 配对胜率 |
|---|---:|---:|---:|---:|---:|---:|
| Balls | 5.1397 ± 0.0496 | 4.5811 ± 0.0123 | 12.2407 ± 0.6171 | 7.2141 ± 0.1407 | 10.86% ± 0.96% | 5/5 |
| David328 | 10.7075 ± 0.1695 | 8.9442 ± 0.1053 | 29.8361 ± 0.7685 | 22.4571 ± 1.0999 | 16.46% ± 0.67% | 5/5 |
| NefertitiFace | 4.4594 ± 0.0339 | 4.0931 ± 0.0077 | 5.7705 ± 0.0861 | 4.5414 ± 0.0214 | 8.21% ± 0.75% | 5/5 |
| Cow | 57.0277 ± 12.1730 | 16.7118 ± 1.1402 | 295.1601 ± 69.0352 | 75.5193 ± 3.5730 | 69.36% ± 8.02% | 5/5 |
| Isis | 7.5398 ± 0.0935 | 5.5311 ± 0.1109 | 18.1805 ± 0.4300 | 10.7512 ± 0.4005 | 26.63% ± 1.99% | 5/5 |

Spline-NVP 在每个模型、每个 seed 上都优于同 seed 的 Affine-NVP，且除 Cow 外方差较小。Cow 上 Affine 的方差显著更大，Spline 同时改善均值和稳定性。完整逐 seed 数据、selected iteration、角度畸变、运行时间和合法率见 `summary.csv`、`statistics.csv` 和 `paired.csv`。

### 5.2 Affine/Spline 公平性说明

v2.1 中 Affine-NVP 与 Spline-NVP 的核心模型骨架一致（6 层耦合、隐层 64、MLP 3 层、1000 iterations、同一目标和初始化），但它们在**网络规模上并不完全对称**。在解读"Spline 优于 Affine"时应留意以下结构性差异：

- **每层 conditioner 输出维度不同**：Affine 每层输出 `(s, t)` 两个标量；Spline 每层输出 `3 × bins − 1` 个样条参数（8 bins 时 = 23 的宽/高/导数，默认 `spline_bins` 为 8）。
- **总参数量不同**：默认配置下 Affine 约 26508 参数，Spline 约 34702 参数（高约 31%）。差异主要来自 conditioner 输出维度的放大，其次是 Spline 额外的全局参数。
- **全局 scale/translation 不对称**：Spline 自带可学习全局正 scale 与 translation（4 个参数），Affine 当时没有该结构（v2.2 起可开启）。
- **映射类型本身不同**：仿射耦合 vs 单调有理二次样条耦合，表达能力类型不同。

Spline 的相对优势是"样条变换本身 + 更高输出维度 + 更多参数 + 全局参数"共同作用的结果。v2.2 随后通过参数匹配消融实验分离这些因素（见 `## 9`）。

### 5.3 Cow 诊断

Cow 是五模型中最大（3195 顶点 / 5804 面）也最难的输入，其失败路径揭示了不同方法对"全局注入性"保证方式的差异。以下为对现有 `data/output/v2.1/seed0/cow/` 结果的诊断性描述：

- **初始 UV 接近退化**：共享 Tutte 初始 UV 的最小 signed area 仅 `5.93e-8`，有 57 个 UV 三角形的面积小于 `1e-6`（涉及约 106 个顶点），初始面积加权 SD 均值约 828、最大约 28 万。这些细长退化三角形对任何轻微顶点位移都极敏感，构成了一个"困难初始化"场景——而不只是一个复杂模型。
- **Direct UV 卡在迭代 0**：训练中 40 次合法检查全部无效（`invalid_rollbacks=40`，`latest_valid_iteration=0`），表现为反复回滚到初始 UV 再重新发散，未能产生任何优于初始的合法解。结合 Direct 直接优化 UV 顶点、缺乏结构性保定向这一特点，可认为它容易在近退化三角形上进入非法区域。
- **SLIM 局部有效但全局重叠**：SLIM 输出 `num_flipped=0`（anti-flip 策略有效），却有 **1069 个非相邻全局相交**（涉及约 467 个面，集中在少数 UV 区域）。这反映了 **local injectivity ≠ global injectivity**：SLIM 能避免三角形局部翻转，但不能保证不同区域在 UV 空间不重叠。
- **Affine/Spline 结构保定向更稳定**：Affine 仅出现 4 次瞬时无效（少量翻面，幅度小）、Spline 仅 2 次，随后均恢复合法；Spline seed-0 SD 约 15.8、多 seed 平均约 16.7±1.14，全部合法。

需要强调的是：上述对失败机制的归因是基于现有结果数据的**诊断性推断**，并非通过额外受控实验证实。NVP 在 Cow 上的稳定性与其**结构性全局可逆 deformation** 的表现一致，但 v2.1 尚未针对初始化质量、网络容量等因素做系统性对照实验。

## 6. 已知限制（v2.1 视角）

- 仍只面向单 connected component、单 boundary loop、disk topology。
- 尚未实现自动切缝、多 chart 和 packing。
- OBJ 内部数据模型仍是一顶点一 UV，不支持 seam 两侧独立 corner UV。
- USD writer 仍是简化写出。
- 全局 triangle intersection validation 在大网格上可能较慢（v2.4 已修复内存问题，见 `## 12`）。
- checkpoint 仍只按 weighted SD loss 选择，没有综合角度和 percentile。
- Direct 和 SLIM 作为确定性 baseline 仅运行 seed 0；Affine/Spline 已覆盖 seeds 0-4。
- `area_weight` 配置名为兼容 v1 保留，但在后续版本中实际控制 Jacobian barrier 权重。

## 7. 下一步（v2.1 时）

建议基于五模型和多 seed 结果继续诊断架构与目标函数：

- Cow Direct iteration-0 停滞。
- SLIM 全局相交出现时刻。
- Affine/Spline 对合法 SLIM target 的监督拟合。

v2.2 已在此基础上推进（公平性消融 + Jacobian/拉伸指标）。

---

# 第二部分：v2.2 记录

## 8. v2.2 主要变化

### 8.1 Affine 全局 scale/translation（`global_transform`）

v2.2 为 `NVP2D` 增加 `global_transform` 开关：

- 启用后，affine 与 spline 一样额外获得可学习全局正 scale 与 translation（各 2 个参数）。
- 这消除了 v2.1 中 affine 无、spline 有的结构不对称，用于公平消融。
- 通过配置键 `model.global_transform`、CLI `--global-transform` 控制；`run_benchmark.py` 支持透传。
- **自 v2.2 起，`global_transform` 默认开启（`configs/default.yaml` / `DEFAULT_CONFIG`）**，即默认的 affine run 即 `affine_g`。

### 8.1bis Spline bins 默认值

**自 v2.2 起，默认 `spline_bins` 从 8 改为 16**，即默认的 spline run 使用 16 bins（效果最好，见 §10）。

### 8.2 新的 Jacobian / 拉伸指标

`metrics.py` 新增基于每个三角形 Jacobian 奇异值（σ1、σ2）的指标：

- `stretch_max_mean` / `stretch_max_area_weighted_mean` / `stretch_max_p95` / `stretch_max_max`：最大拉伸 σ_max。
- `condition_number_mean` / `condition_number_area_weighted_mean` / `condition_number_p95`：条件数 κ = σ_max / σ_min。

这些字段已加入 `summary.py` 的 `SUMMARY_FIELDS` 与多 seed 统计 `summarize_benchmark_seeds.py` 的 `METRICS`。

### 8.3 基准透传模型参数

`run_benchmark.py` 支持传递模型参数（`--num-layers`、`--hidden-dim`、`--mlp-layers`、`--spline-bins`、`--global-transform`），并写入 benchmark manifest 签名，保证可复现。

## 9. v2.2 公平性实验结果

v2.2 在 **seed 0、1000 iterations、CUDA、同一 Tutte 初始 UV、geometry-scale** 下，对 5 个模型只运行两种 NVP（无 Direct/SLIM），对比 4 个配置：

| 配置 | 耦合 | hidden | 全局参数 | 参数量 |
|---|---|---:|---|---:|
| `affine` | affine | 64 | 关 | 26508 |
| `affine_g` | affine | 64 | 开 | 26512 |
| `affine_cap` | affine | 74 | 开 | 35092 |
| `spline` | spline | 64 | 开(内建) | 34702 |

结果输出在 `data/output/v2.2/`（每配置独立 benchmark 根目录 + manifest），合并汇总见 `data/output/v2.2/v2.2_summary.json`。

### 9.1 面积加权对称 Dirichlet SD（seed 0）

| 模型 | affine | affine_g | affine_cap | spline |
|---|---:|---:|---:|---:|
| Balls | 5.202 | 5.169 | 5.107 | **4.570** |
| David328 | 10.599 | 10.463 | 10.590 | **8.896** |
| NefertitiFace | 4.433 | 4.427 | 4.456 | **4.097** |
| Isis | 7.636 | 7.623 | 7.649 | **5.554** |
| Cow | 67.758 | 50.704 | 56.917 | **15.795** |

全部 20 次运行均合法（0 flip、0 intersection）。

### 9.2 拉伸 σ_max 与条件数 κ（面积加权）

| 模型 | affine σmax | spline σmax | affine κ | spline κ |
|---|---:|---:|---:|---:|
| Balls | 1.306 | **1.225** | 1.669 | **1.496** |
| David328 | 1.837 | **1.712** | 3.201 | **2.921** |
| NefertitiFace | 1.181 | **1.081** | 1.382 | **1.168** |
| Isis | 1.632 | **1.378** | 2.628 | **1.902** |
| Cow | 4.399 | **2.052** | 5.937 | **3.549** |

### 9.3 结论

- **全局 scale/translation 单独作用有限**（`affine_g` vs `affine`）：多数模型几乎无差别，仅在 **Cow** 上明显改善 SD（67.76 → 50.70）。
- **容量对齐后 spline 仍占优**（`affine_cap` ≈35092 vs `spline` ≈34702）：参数量相近时，spline 在所有模型、所有指标上仍显著更优（Cow SD 56.92 vs 15.79；Isis 7.65 vs 5.55）。这**强烈表明 Spline 的优势主要来自样条变换本身的表达能力，而非单纯更多参数**。
- **增加 affine 容量帮助有限**（`affine_cap` 多数模型持平甚至略差），说明 affine 的瓶颈是映射类型而非容量。
- **拉伸与条件数一致**：spline 在所有模型上 σ_max、κ 均匀更低，畸变分布更均匀、长尾更小。

## 10. v2.2 Spline bins 消融

在 seed 0、5 模型上对比 `spline_bins = 4 / 8 / 16`（其余配置一致，hidden 64）。bins=4、16 的结果在 `data/output/v2.2/spline_bins4/`、`spline_bins16/`，bins=8 复用 `spline/`。全部 15 次运行合法（0 flip、0 intersection），合并汇总见 `data/output/v2.2/v2.2_bins_summary.json`。

### 10.1 面积加权对称 Dirichlet SD（seed 0）

| 模型 | bins=4 | bins=8 | bins=16 |
|---|---:|---:|---:|
| Balls | 4.759 | 4.570 | **4.502** |
| David328 | 9.765 | 8.896 | **8.516** |
| NefertitiFace | 4.192 | 4.097 | **4.058** |
| Isis | 6.217 | 5.554 | **5.365** |
| Cow | 28.324 | 15.795 | **12.807** |

### 10.2 拉伸 σ_max 与条件数 κ（面积加权）

| 模型 | σmax b4 | σmax b8 | σmax b16 | κ b4 | κ b8 | κ b16 |
|---|---:|---:|---:|---:|---:|---:|
| Balls | 1.264 | 1.225 | **1.210** | 1.587 | 1.496 | **1.456** |
| David328 | 1.799 | 1.712 | **1.689** | 3.109 | 2.921 | **2.857** |
| NefertitiFace | 1.118 | 1.081 | **1.062** | 1.247 | 1.168 | **1.128** |
| Isis | 1.482 | 1.378 | **1.334** | 2.139 | 1.902 | **1.809** |
| Cow | 2.778 | 2.052 | **1.955** | 4.713 | 3.549 | **3.410** |

### 10.3 结论

- **增加 bins（4→8→16）在所有模型、所有指标上单调改善**：SD、σ_max、κ 均下降，训练时间基本不变。
- 改善幅度随 bins 增大而**递减**（4→8 的改善大于 8→16），表达能力存在边际递减。
- **Cow 受益最大**（SD 28.32 → 15.80 → 12.81），进一步印证样条表达能力在困难网格上价值更大。

---

# 第三部分：v2.4 记录

## 11. v2.4 背景与问题

v2.4 的核心改动是**全局三角形相交校验的可扩展性修复**，使 pipeline 可以在更大的网格（几万面乃至更多）上运行，不再因显存溢出（OOM）而无法进行全局合法性校验。

在 v2.2 及之前，全局非相邻三角形相交校验 `count_triangle_intersections_torch`（GPU）在**校验前**先把全部 `O(F²)` 个三角形对物化为端上张量：

```python
pair_i, pair_j = torch.triu_indices(num_faces, num_faces, offset=1, ...)  # O(F²) 索引
fi = faces[pair_i]   # [n_pairs, 3]
fj = faces[pair_j]   # [n_pairs, 3]
adjacent = (fi[:, :, None] == fj[:, None, :]).any(dim=(1, 2))
```

对 `F` 个面的网格，`n_pairs ≈ F²/2`。例如 `F = 26408` 时 `n_pairs ≈ 3.5 亿`，仅 `fi`/`fj` 两个张量就约 **16 GB**，在 8 GB 的 GPU 上直接 OOM。

- OOM 由**面数 F** 驱动，与是否有 seam/xatlas 无关（seam 不会减少 UV 三角形数量 ≈ F）。
- CPU 版 `find_triangle_intersections` 是 Python 双层循环 O(F²)，在几万面上时间上不可行。

这导致 pipeline 在较大的测试网格（如 `data/input/00027/Input.obj`，13893 顶点 / 26408 面）上无法完成任何方法的校验。

## 12. v2.4 改动内容

### 12.1 GPU 版（`count_triangle_intersections_torch`）——块式处理

将"先生成全量 pair → 过滤相邻 → 再批处理"改为**逐块枚举**：

- 用 `tile_size`（默认 1024）把面索引划分成**上三角块**，逐块生成该块的 `(i, j)` pair（对角块用 `triu_indices`，非对角块用 `meshgrid`）。
- 在**每个块内**完成：相邻过滤 → bbox 剪枝 → 精确线段/点相交测试（仍按 `batch_size` 分批）。
- **内存稳定在 O(tile² + batch)**，不再物化 O(F²) 全对张量。

**语义完全不变**：仍是严格的全局检测——每个非相邻、bbox 重叠的三角形对都会被精确测试。仅改变了内存布局和计算调度。

接口新增参数 `tile_size`（默认 1024），其余参数与返回值不变。

### 12.2 CPU 版（`find_triangle_intersections`）——块式处理

用同样的逐块枚举替代 Python 双层循环，在块内用向量化的 numpy 做相邻过滤与 bbox 剪枝，只对幸存 pair 做精确测试：

- 内存有界（逐块，不建 O(F²) 数组）。
- 时间：O(F²) 对在最坏情况下仍需逐个向量化处理，但内存不再成为瓶颈，且 bbox 剪枝可大幅减少精确测试数量。

接口新增参数 `tile_size`（默认 512）。

## 13. v2.4 验证

### 13.1 正确性（与暴力法一致）

- 在 `NefertitiFace`（562 面）上做多组轻扰动 UV，含 800~1500 对真实相交：
  - **CPU、torch、暴力法三方结果完全一致**（6 组、每组 811~1469 对，全部相等）。
- 合成用例（小三角形嵌在大三角形内，非相邻重叠）：CPU 与 torch 均正确检出。

### 13.2 大网格可扩展性（`00027/Input.obj`，26408 面）

| 场景 | GPU 结果 | GPU 时间 | GPU 峰值显存 | CPU 结果 | CPU 时间 |
|---|---:|---:|---:|---:|---:|
| Tutte 初始 UV | 0 相交 | 18.2 s | **116 MB**（此前 OOM >16GB） | 0 相交 | 74 s |
| 网格自带 UV | 780 相交 | 10.0 s | **116 MB** | 780 相交 | 79 s |

- **不再 OOM**：峰值显存从 >16 GB 降到 **116 MB**，26408 面稳定可跑。
- CPU 与 GPU 结果一致（Tutte 0 对、自带 UV 780 对）。

### 13.3 回归

- `pytest` 测试套件：**30/30 全部通过**。

## 14. v2.4 已知限制与后续

- 仍是 O(F²) 的**最坏时间**（bbox 重叠时），GPU 约 10~18 s、CPU 约 75~80 s（26408 面）。对训练中每 checkpoint 校验一次可能累积耗时。
- 后续可在其上加**空间栅格/空间索引候选缩减**，把候选从 O(F²) 降到近线性，以支撑更大网格（10 万+ 面）与未来 xatlas 后的复杂网格。当前版本以"内存有界、语义严格"为优先。

## 15. v2.4 涉及的接口

- `surface_nvp/injectivity/triangle_intersection.py`：
  - `count_triangle_intersections_torch(uv, faces, ignore_adjacent=True, batch_size=262144, tile_size=1024, eps=1e-12)`
  - `find_triangle_intersections(uv, faces, ignore_adjacent=True, tile_size=512)`
- 调用方（`validators.py` 的 `validate_uv_torch` / `validate_uv`）接口签名不变，无需改动。

## 16. 版本与默认配置一致性

发布审计发现并修复了两处版本一致性问题：

- `pyproject.toml` 与 `surface_nvp/__init__.py` 统一为 `2.4.0`。
- `global_transform` 在 `NVP2D`、训练入口、默认 YAML 与 benchmark CLI 中统一为 `true`；benchmark 现在显式透传 `--global-transform` 或 `--no-global-transform`，不会再因 CLI 的旧 `store_true` 默认值覆盖配置。
- 默认 `spline_bins` 在模型、训练、监督拟合和 CLI 中统一为 16。

`data/output/v2.4/{Balls,David328,NefertitiFace,Isis,Cow,00027}/manifest.json` 是修复 benchmark 透传问题之前生成的历史 artifact，其中 `global_transform=false`、`spline_bins=null` 是 **manifest 记录错误**。各运行保存的 `*.config.json` 与实际模型配置表明 `global_transform=true`、`spline_bins=16`。这些历史 manifest 没有被事后改写，以保留原始 provenance；新 benchmark 会记录正确的 effective settings。

## 17. 00027 诊断

`data/input/00027/Input.obj` 含 13893 顶点、26408 三角形。拓扑检查结果为：单连通分量、Euler characteristic 1、一个 1376 顶点边界环、无非流形边和孤立顶点。因此它仍是 pipeline 支持的单盘拓扑；困难主要来自细长肢体、复杂边界和几何尺度，而不是 genus 或多边界等复杂拓扑。

### 17.1 初始 UV

- Tutte 初值合法，但面积加权 SD 为 `978.4236`，SD P95 为 `2565.45`，最小 signed area 约 `5.28e-8`；700 个 UV 三角形面积低于 `1e-6`，2464 个低于 `1e-5`。
- `Input.obj` 自带 UV 的面积加权 SD 约 `4.0158`、条件数约 `1.0305`、0 flip，但存在约 780 个全局非相邻相交，因此不能作为合法初值。
- `Output_No_Gap.obj` / `Output_With_Gap.obj` 使用 seam/corner UV，顶点和面拓扑分别为 14627/27867，与当前 one-UV-per-vertex 输入拓扑不一致，不能直接作为共享初值。

### 17.2 v2.4 正式结果

| 方法 | 面积加权 SD | 选择迭代 | flip | 全局相交 | 合法 |
|---|---:|---:|---:|---:|---|
| Initial | 978.4236 | 0 | 0 | 0 | 是 |
| Direct UV | 978.4236 | 0 | 0 | 0 | 是 |
| Affine-NVP | 112.8912 | 1000 | 0 | 0 | 是 |
| Spline-NVP | 978.4224 | 0 | 0 | 0 | 是 |
| SLIM | 4.0184 | 20 | 0 | 0 | 是 |

Direct 与 Spline 的失败轨迹暴露了旧 rollback 的学习率恢复问题：恢复 optimizer checkpoint 后再衰减会反复回到同一基准学习率，导致每次非法检查后的学习率停留在 `5e-4`，而不是累计降低。00027 的近退化 Tutte 三角形对步长极其敏感；例如 Spline 在 `1e-3` 首步产生数百 flip，而 `1e-5` 才能连续保持局部合法。该问题在 §18 的累计衰减修复中解决。

## 18. 优化 1+2+3

v2.4 进一步加入三组可选改进，默认配置保持基线行为：

1. **训练策略**：修复 rollback 学习率累计衰减；新增 `constant|cosine` Adam schedule；新增可选 PyTorch L-BFGS 收尾阶段。
2. **可逆 mixing**：耦合层之间可加入可学习二维旋转。旋转 determinant 为 +1、logdet 为 0，并有解析 inverse；默认 `mixing_type=none`。
3. **更大 Spline-NVP**：`configs/improved_123.yaml` 使用 12 层、hidden 128、32 bins、rotation mixing、3000 Adam + 50 L-BFGS；它是显式实验配置，不是默认配置。

1000 Adam + 20 L-BFGS 的统一预算结果如下：

| 模型 | v2.4 Spline16 SD | Improved SD | 相对变化 | SLIM SD | 合法 |
|---|---:|---:|---:|---:|---|
| Balls | 4.502016 | 4.483172 | -0.42% | 4.245895 | 是 |
| David328 | 8.516175 | 8.163346 | -4.14% | 7.237717 | 是 |
| NefertitiFace | 4.058399 | 4.068797 | +0.26% | 4.036175 | 是 |
| Isis | 5.365119 | 5.270774 | -1.76% | 4.530849 | 是 |

完整预算的 David328 达到 SD `7.877279`，比旧 Spline16 改善 7.50%，与 SLIM 的差距缩小到 8.84%。训练在 iteration 700 出现一次非法 checkpoint，累计学习率衰减后恢复并保持合法至 iteration 3050，验证了 rollback 修复。L-BFGS 在现有配置下保持合法但没有改善 best checkpoint，因此仍默认关闭。

结论是：更长 cosine Adam、较大模型与 rotation mixing 对较难的 David328/Isis 有帮助，但不是所有简单网格都受益，且成本约为基线的两倍。因此 v2.4 默认仍为 6 层、hidden 64、16 bins、无 mixing、1000-step constant Adam、无 L-BFGS。

## 19. 合法初值扩展

初始化接口新增：

- `tutte`：原有弦长凸边界 + 均匀正权重，继续作为默认值。
- `mean_value`：弦长凸边界 + Floater 正 mean-value 权重；对满足要求的盘状三角网格保留凸组合映射的合法性优势。
- `abfpp`：通过固定 OpenABF v2.1.0 的可选 C++ runner 计算 ABF++ 角度并用 angle-based LSCM 重建坐标。
- `auto`：生成 Tutte、mean-value 和可用的 ABF++；先做 geometry scaling，再用 v2.4 validator 检查 nonfinite、flip 和全局相交，只在完全合法的候选中选择面积加权 SD 最低者。

ABF++ 文献中的 validity 指局部无翻转，不代表本项目要求的全局无重叠。因此最终保证来自“候选生成 + v2.4 完整检测 + 凸边界兜底”，而不是单独依赖 ABF++。显式 `abfpp` 失败时会报错；`auto` 会记录错误并继续尝试其他候选。`scripts/init_uv.py` 同时写出 `*.init.json`，保存请求方法、最终选择和所有候选指标。

发布前候选测试如下；表中为 geometry-scaled 面积加权 SD：

| 模型 | Tutte | Mean-value | ABF++ | `auto` 选择 |
|---|---:|---:|---:|---|
| Balls | 5.7546 | **5.0765** | 6.7685 | mean-value |
| David328 | **28.4099** | 33.8629 | 61.8357 | Tutte |
| NefertitiFace | 5.3088 | 4.1691 | **4.0914** | ABF++ |
| Isis | 13.4625 | **9.7803** | 12.1945 | mean-value |
| Cow | **285.9815** | 329.8874 | 非法：109 相交 | Tutte |
| 00027 | 978.4236 | **899.4890** | 非法：651 相交 | mean-value |

00027 的 mean-value 初值相对 Tutte 降低约 8.1%，同时保持 0 flip、0 intersection；ABF++ 虽有 0 flip 和更大的最小 signed area，却因 651 个全局相交被正确拒绝。这再次验证局部合法不等于全局合法。

上述候选测试用于验证新初始化代码，尚未替换 `data/output/v2.4/` 中基于 Tutte 的正式优化结果。后续若比较新初值对 NVP 的影响，应使用新的 benchmark output root，避免与当前正式基线混写。

## 20. v2.4 发布状态

### 20.1 默认配置

```yaml
init:
  method: tutte
  boundary: circle
  geometry_scale: true
  abfpp_executable: null
model:
  coupling_type: affine
  num_layers: 6
  hidden_dim: 64
  mlp_layers: 3
  spline_bins: 16
  spline_bound: 1.1
  global_transform: true
  mixing_type: none
train:
  iters: 1000
  lr: 0.001
  lr_schedule: constant
  lbfgs_iters: 0
  identity_weight: 0.001
  area_weight: 100.0
```

### 20.2 发布验证

- Python tests：40 passed。
- `python -m compileall -q surface_nvp scripts`：通过。
- `git diff --check`：通过（仅 Windows CRLF 提示）。
- OpenABF v2.1.0 runner：Windows / Visual Studio 2022 Release 编译及真实网格调用通过。
- Mean-value 与 `auto`：六个数据集完整合法性筛选通过。
- 00027：Tutte、mean-value、ABF++ 三候选完成严格 CPU 全局检测；ABF++ 的 651 个相交被拒绝。

### 20.3 发布归档

- `data/output/v2.4/{Balls,David328,NefertitiFace,Isis,Cow,00027}/`：Tutte 初值下的 Direct/Affine/Spline/SLIM 正式结果。
- `data/output/v2.4/improved_123/eval/`：四个简单网格的统一预算实验。
- `data/output/v2.4/improved_123/full/David328/`：完整预算验证。
- `configs/improved_123.yaml`：可复用实验配置。

一次性的 100-step NefertitiFace smoke artifact 不纳入发布归档；实现正确性由测试、正式 eval 和 full run 覆盖。
